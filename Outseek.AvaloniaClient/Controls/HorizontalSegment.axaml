<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Outseek.AvaloniaClient.Controls">
    <Design.PreviewWith>
        <Border Padding="2">
            <controls:HorizontalSegment
                From="20" To="50" IsEditable="True" Step="5"
                MinLength="10"
                IsContinuousUpdate="False"
                Width="200">
                <Rectangle Stroke="Red" StrokeThickness="1" Height="30" />
            </controls:HorizontalSegment>
        </Border>
    </Design.PreviewWith>

    <Style Selector="controls|HorizontalSegment">
        <Setter Property="Template">
            <ControlTemplate>
                <!-- the grid's column definitions get manipulated at runtime -->
                <Grid Name="PART_ContainerGrid" HorizontalAlignment="Stretch" ColumnDefinitions="*,0,*,0,*">
                    <!-- column definition for this represents "From" -->
                    <Panel Grid.Column="0" />

                    <GridSplitter Grid.Column="1"
                                  Name="PART_LeftSplitter"
                                  Width="3"
                                  ZIndex="1" />

                    <!-- column definition for this represents "To" (derived from the duration) -->
                    <ContentPresenter Grid.Column="2"
                                      Content="{TemplateBinding Content}"
                                      ClipToBounds="True"
                                      ContentTemplate="{TemplateBinding ContentTemplate}" />

                    <GridSplitter Grid.Column="3"
                                  Name="PART_RightSplitter"
                                  Width="3"
                                  ZIndex="1" />

                    <!-- column definition for this is star-sized to just fill the remaining space -->
                    <Panel Grid.Column="4" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style Selector="controls|HorizontalSegment GridSplitter">
        <Setter Property="Background" Value="#00000000" />
    </Style>
    <Style Selector="controls|HorizontalSegment GridSplitter:pointerover">
        <Setter Property="Background" Value="#66ffffff" />
    </Style>

</Styles>
